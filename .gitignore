# Client source documents — internal planning material, not for a public repo.
# Anchored to the repo root: an unanchored *.jpg also swallowed the site's own
# photographs (site/assets/img/), so they never reached the deploy.
/*.docx
/*.pdf
/*.jpeg
/*.jpg

# Local tooling
.claude/

# Admin panel backups
admin/*.bak
site/**/*.bak

# Local Netlify folder
.netlify

# macOS junk
.DS_Store
